# イベントカレンダー（静的版）

HTML/CSS/JSだけで動く版です。  
イベントはブラウザの `localStorage` に保存されます。

## 使い方

- `index.html` をブラウザで開く
- 右フォームで追加・編集・削除
- 上部の `JSONを書き出し` でデータをファイル保存
- 上部の `JSONを読み込み` で配布されたイベントデータを反映

## 共有運用の考え方

- 各端末のデータは自動同期されません
- 共有したい場合は、担当者がJSONを書き出して配布し、各メンバーが読み込みます
